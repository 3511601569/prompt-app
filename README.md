# AI 提示词优化器

基于 Next.js (App Router) 和 Tailwind CSS 的 AI 提示词优化器，将简单的需求描述优化为专业的 Midjourney 提示词。

## 功能特性

- 🎨 极简现代的深色系 UI 设计
- 📱 响应式布局（桌面端左右分栏，移动端上下排列）
- 🤖 使用 DeepSeek API 进行提示词优化
- ⚡ 基于 Next.js 14 App Router

## 快速开始

### 1. 安装依赖

```bash
npm install
```

### 2. 配置环境变量

创建 `.env.local` 文件并添加你的 DeepSeek API Key：

```env
DEEPSEEK_API_KEY=your_deepseek_api_key_here
```

### 3. 运行开发服务器

```bash
npm run dev
```

在浏览器中打开 [http://localhost:3000](http://localhost:3000) 查看应用。

## 项目结构

```
prompt-app/
├── app/
│   ├── api/
│   │   └── optimize/
│   │       └── route.ts      # API 路由
│   ├── globals.css           # 全局样式
│   ├── layout.tsx            # 根布局
│   └── page.tsx              # 主页面
├── .env.local                # 环境变量（需要创建）
├── next.config.js            # Next.js 配置
├── tailwind.config.js        # Tailwind CSS 配置
└── package.json
```

## 使用说明

1. 在左侧输入框中输入你的简单需求（如"画一只猫"）
2. 点击中间的"生成/优化"按钮
3. 优化后的 Midjourney 提示词会显示在右侧
4. 可以点击"复制"按钮复制优化后的提示词

## 构建和部署

```bash
# 构建生产版本
npm run build

# 启动生产服务器
npm start
```

## 技术栈

- **Next.js 14** - React 框架（App Router）
- **TypeScript** - 类型安全
- **Tailwind CSS** - 样式框架
- **OpenAI SDK** - 用于调用 DeepSeek API
